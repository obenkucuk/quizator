import 'package:flutter/material.dart';

class QuizCategoriesView extends StatelessWidget {
  const QuizCategoriesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text('QuizCategoriesView'),
    ));
  }
}
